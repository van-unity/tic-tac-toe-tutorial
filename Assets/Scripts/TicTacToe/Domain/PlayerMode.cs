namespace TicTacToe.Domain {
    /// <summary>
    /// Defines the modes in which a player can operate in the TicTacToe game.
    /// </summary>
    public enum PlayerMode {
        Manual = 0,
        Auto = 1
    }
}